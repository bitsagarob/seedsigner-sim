# The wallet this simulator runs, pinned.
#
# src/seedsigner in the built wallet.zip is this tree verbatim. Nothing in this
# repository patches it: the four hardware seams are replaced from the outside,
# by the modules in src/shims. That is the property the build script exists to
# let you check for yourself.
#
# A release tag rather than a branch tip. A tip moves: rebase or force-push dev
# and the sha below stops existing on the remote, at which point every rebuild
# described here fails at the fetch, for everyone, permanently. A tag is a name
# upstream has published and does not move. This one is also what the official
# pi0-smartcard device image is built from, so the simulator and the physical
# device run the same code. The sha is still the pin; the tag only says which
# release it is.
repo   = https://github.com/3rdIteration/seedsigner.git
commit = 662d9dba2327eb77d6924ae9bd62d4902bf24634
tag    = SeSi-0.8.7+ShSi-B11

# The wallet.zip that build/build-wallet-zip.sh produces from the pin above.
# Rebuild it yourself and compare; if these differ, something between that commit
# and the file you were served is not what it claims to be.
wallet_zip_sha256      = d872779967ed1e2b400de469590dd7bf3a459db66c071d26211e87f866db3199
wallet_zip_contents_sha256 = d2e658a2a422c8d2a318ec0f1159d1a29750b1862b27fd2cf59c336bf9376180
